# Fix ledger — {{PROJECT}}

> One section per report (newest first); each carries status → root cause → fix → files →
> verification → commit. Kept in lockstep with `../report/report.md` — **update in the SAME change
> as the fix** (§4.8).

| # | Report | Status | Commit(s) |
|---|--------|--------|-----------|
| 1 | {{short}} | `open` | — |

---
## §1 — {{title}} (B1) — `{{status}}`
**Root cause:** {{…}} · **Fix:** {{…}} · **Files:** `{{…}}` · **Verification:** {{observable check}}
· **Commit:** `{{sha}}`.
