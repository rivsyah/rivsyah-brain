# {{PROJECT}} v1 — DECISIONS (locked, append-only)

> Final word on settled questions. Overrides stale wording in build files / GRANDPLAN. **Append;
> don't rewrite history.** To change a locked decision: add a NEW entry that supersedes it, mark the
> old `superseded` in place. v1 IDs `D…`; later versions `VN-D…`. Lock only with the owner.

## Carry-over (earlier decisions still in force)
<!-- filled on version freeze, e.g.  - D4 — {{title}} (see ../../v(N-1)/implementation/DECISIONS.md). -->
- {{none — v1}}

## Decisions
### D1 — {{title}} — `proposed` ({{date}})
{{the decision}}. **Why:** {{…}}. **Where it bites:** {{files/behavior}}. **Supersedes:** none.
