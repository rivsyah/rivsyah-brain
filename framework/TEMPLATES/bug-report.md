# Bug Reports — {{PROJECT}}

> Owner's raw inbox — the agent maintains it (§4.8: any "broken/wrong/failing/missing" in chat
> becomes a `B<n>` here BEFORE the fix starts). Each report gets a `Status:` line and (if a real
> code fix) a write-up in `../fix/report-fixes.md`, kept in lockstep.
> **`--polished`:** a tagged report is already cleaned — leave it. Untagged is fair game (keep intent).
> **Status:** open · investigating · fixed · external · by-design · wontfix.

## Status at a glance
| # | Report | Status | Where |
|---|--------|--------|-------|
| 1 | {{short}} | `open` | fix ledger §1 |

---
## B1 — {{short title}}
{{the report}}
**Status:** `open` — {{one line}}
